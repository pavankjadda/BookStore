kind: PersistentVolume
apiVersion: v1
metadata:
  name: sonarqube-data
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/opt/sonarqube/data"
