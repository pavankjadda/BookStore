kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: sonarqube-postgresql
  namespace: cicd
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
