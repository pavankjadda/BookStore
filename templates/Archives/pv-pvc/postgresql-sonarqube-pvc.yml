apiVersion: "v1"
kind: "PersistentVolumeClaim"
metadata:
  name: "postgresql-sonarqube"
spec:
  storageClassName: manual
  accessModes:
    - "ReadWriteOnce"
  resources:
    requests:
      storage: "4Gi"
  volumeName: "postgresql-sonarqube"
